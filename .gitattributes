# Normalise line endings to LF in the repo so shell scripts and configs
# work inside the Linux Docker images regardless of the host OS.
* text=auto eol=lf

*.png binary
*.jpg binary
*.docx binary
*.pdf binary
